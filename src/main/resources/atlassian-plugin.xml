<atlassian-plugin key="${project.groupId}" name="${project.description}" plugins-version="2">
	<plugin-info>
		<description>${project.description}</description>
		<version>${project.version}</version>
		<vendor name="${project.organization.name}" url="${project.organization.url}" />
	</plugin-info>

	<repository-hook key="${project.groupId}" name="Flowdock Post-Receive Notification" class="${project.groupId}.FlowdockPostReceiveNotificationHook">
		<description>${project.description}</description>
		<icon>icons/flowdock-black.png</icon>
		<config-form name="Flowdock Post-Receive Notification Config" key="${project.groupId}">
			<view>${project.groupId}</view>
			<directory location="/static/" />
		</config-form>
	</repository-hook>
</atlassian-plugin>
